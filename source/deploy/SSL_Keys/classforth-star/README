star-classforth.crt is the ssl certificate, which has the following:
1. root certificate
2. intermediate certificate
3. private key
The order matters.

star-classforth.csr is the application form when generating certificate.

The cert is for *.classforth.com, no secondary subdomain is allowed.

private-key.pem is the private key.
