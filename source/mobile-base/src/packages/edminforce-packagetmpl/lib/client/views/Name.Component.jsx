{

    // Don't forget to change `SomeName` to correct name
    EdminForce.Components.SomeName = React.createClass({

        render: function () {

            // Fill with your UI
            return (
                <div>

                </div>
            );
        }
    });

}