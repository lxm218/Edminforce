{

    // Don't forget to change `SomeName` to correct name
    EdminForce.Components.ClassesSummary = React.createClass({

        render: function () {

            // Fill with your UI
            return (
                <div>
                    Class summary
                </div>
            );
        }
    });

}