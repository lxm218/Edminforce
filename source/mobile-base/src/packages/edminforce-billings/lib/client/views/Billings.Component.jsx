{

    // Don't forget to change `SomeName` to correct name
    EdminForce.Components.Billings = React.createClass({

        render: function () {

            // Fill with your UI
            return (
                <div>
                    <h3>Billings</h3>
                </div>
            );
        }
    });

}